import time

start_time = time.time()
end_time = start_time + 3600  

while time.time() < end_time:
    current_time = time.strftime("%M:%S:%s", time.gmtime(time.time() - start_time))
    print(current_time)
    time.sleep(1)
